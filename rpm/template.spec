Name:           ros-indigo-network-control-tests
Version:        1.0.10
Release:        0%{?dist}
Summary:        ROS network_control_tests package

Group:          Development/Libraries
License:        BSD
URL:            http://ros.org/wiki/network_control_tests
Source0:        %{name}-%{version}.tar.gz

Requires:       ros-indigo-access-point-control
Requires:       ros-indigo-ddwrt-access-point
Requires:       ros-indigo-dynamic-reconfigure
Requires:       ros-indigo-hostapd-access-point
Requires:       ros-indigo-linksys-access-point
Requires:       ros-indigo-network-monitor-udp
Requires:       ros-indigo-network-traffic-control
Requires:       ros-indigo-rostest
BuildRequires:  ros-indigo-access-point-control
BuildRequires:  ros-indigo-catkin
BuildRequires:  ros-indigo-ddwrt-access-point
BuildRequires:  ros-indigo-dynamic-reconfigure
BuildRequires:  ros-indigo-hostapd-access-point
BuildRequires:  ros-indigo-linksys-access-point
BuildRequires:  ros-indigo-network-monitor-udp
BuildRequires:  ros-indigo-network-traffic-control
BuildRequires:  ros-indigo-rostest

%description
Test suite for the packages that are part of the &quot;WiFi Test Setup&quot;
project: network_monitor_udp, network_traffic_control, hostapd_access_point,
linksys_access_point, ddwrt_access_point.

%prep
%setup -q

%build
# In case we're installing to a non-standard location, look for a setup.sh
# in the install tree that was dropped by catkin, and source it.  It will
# set things like CMAKE_PREFIX_PATH, PKG_CONFIG_PATH, and PYTHONPATH.
if [ -f "/opt/ros/indigo/setup.sh" ]; then . "/opt/ros/indigo/setup.sh"; fi
mkdir -p obj-%{_target_platform} && cd obj-%{_target_platform}
%cmake .. \
        -UINCLUDE_INSTALL_DIR \
        -ULIB_INSTALL_DIR \
        -USYSCONF_INSTALL_DIR \
        -USHARE_INSTALL_PREFIX \
        -ULIB_SUFFIX \
        -DCMAKE_INSTALL_PREFIX="/opt/ros/indigo" \
        -DCMAKE_PREFIX_PATH="/opt/ros/indigo" \
        -DSETUPTOOLS_DEB_LAYOUT=OFF \
        -DCATKIN_BUILD_BINARY_PACKAGE="1" \

make %{?_smp_mflags}

%install
# In case we're installing to a non-standard location, look for a setup.sh
# in the install tree that was dropped by catkin, and source it.  It will
# set things like CMAKE_PREFIX_PATH, PKG_CONFIG_PATH, and PYTHONPATH.
if [ -f "/opt/ros/indigo/setup.sh" ]; then . "/opt/ros/indigo/setup.sh"; fi
cd obj-%{_target_platform}
make %{?_smp_mflags} install DESTDIR=%{buildroot}

%files
/opt/ros/indigo

%changelog
* Wed Sep 16 2015 Dash <dash@clearpathrobotics.com> - 1.0.10-0
- Autogenerated by Bloom

