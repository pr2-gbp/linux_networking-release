%bcond_without weak_deps

%global __os_install_post %(echo '%{__os_install_post}' | sed -e 's!/usr/lib[^[:space:]]*/brp-python-bytecompile[[:space:]].*$!!g')
%global __provides_exclude_from ^/opt/ros/noetic/.*$
%global __requires_exclude_from ^/opt/ros/noetic/.*$

Name:           ros-noetic-network-control-tests
Version:        1.1.0
Release:        1%{?dist}%{?release_suffix}
Summary:        ROS network_control_tests package

License:        BSD
URL:            http://ros.org/wiki/network_control_tests
Source0:        %{name}-%{version}.tar.gz

Requires:       ros-noetic-access-point-control
Requires:       ros-noetic-ddwrt-access-point
Requires:       ros-noetic-dynamic-reconfigure
Requires:       ros-noetic-hostapd-access-point
Requires:       ros-noetic-linksys-access-point
Requires:       ros-noetic-network-monitor-udp
Requires:       ros-noetic-network-traffic-control
Requires:       ros-noetic-rostest
BuildRequires:  ros-noetic-access-point-control
BuildRequires:  ros-noetic-catkin
BuildRequires:  ros-noetic-ddwrt-access-point
BuildRequires:  ros-noetic-dynamic-reconfigure
BuildRequires:  ros-noetic-hostapd-access-point
BuildRequires:  ros-noetic-linksys-access-point
BuildRequires:  ros-noetic-network-monitor-udp
BuildRequires:  ros-noetic-network-traffic-control
BuildRequires:  ros-noetic-rostest
Provides:       %{name}-devel = %{version}-%{release}
Provides:       %{name}-doc = %{version}-%{release}
Provides:       %{name}-runtime = %{version}-%{release}

%description
Test suite for the packages that are part of the &quot;WiFi Test Setup&quot;
project: network_monitor_udp, network_traffic_control, hostapd_access_point,
linksys_access_point, ddwrt_access_point.

%prep
%autosetup

%build
# In case we're installing to a non-standard location, look for a setup.sh
# in the install tree that was dropped by catkin, and source it.  It will
# set things like CMAKE_PREFIX_PATH, PKG_CONFIG_PATH, and PYTHONPATH.
if [ -f "/opt/ros/noetic/setup.sh" ]; then . "/opt/ros/noetic/setup.sh"; fi
mkdir -p obj-%{_target_platform} && cd obj-%{_target_platform}
%cmake3 \
    -UINCLUDE_INSTALL_DIR \
    -ULIB_INSTALL_DIR \
    -USYSCONF_INSTALL_DIR \
    -USHARE_INSTALL_PREFIX \
    -ULIB_SUFFIX \
    -DCMAKE_INSTALL_LIBDIR="lib" \
    -DCMAKE_INSTALL_PREFIX="/opt/ros/noetic" \
    -DCMAKE_PREFIX_PATH="/opt/ros/noetic" \
    -DSETUPTOOLS_DEB_LAYOUT=OFF \
    -DCATKIN_BUILD_BINARY_PACKAGE="1" \
    ..

%make_build

%install
# In case we're installing to a non-standard location, look for a setup.sh
# in the install tree that was dropped by catkin, and source it.  It will
# set things like CMAKE_PREFIX_PATH, PKG_CONFIG_PATH, and PYTHONPATH.
if [ -f "/opt/ros/noetic/setup.sh" ]; then . "/opt/ros/noetic/setup.sh"; fi
%make_install -C obj-%{_target_platform}

%files
/opt/ros/noetic

%changelog
* Tue Mar 02 2021 Devon Ash <dash@clearpathrobotics.com> - 1.1.0-1
- Autogenerated by Bloom

